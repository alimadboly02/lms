
  # lms-lms

  This is a code bundle for lms-lms. The original project is available at https://www.figma.com/design/KCAQqdv3pg7AiargOlTH4Y/lms-lms.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  